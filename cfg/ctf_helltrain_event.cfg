exec base/config_ctf
exec config_ctf
exec config_halloween
script Convars.SetValue("tf_halloween_gift_lifetime", "0");
script DoEntFire("tf_halloween_gift_spawn_location", "Kill", "0", 0, null, null);
script DoEntFire("tf_logic_holiday", "HalloweenSetUsingSpells", "1", 0, null, null); // this enables spells immediately when the map starts, something the base map erroneously does not do

// script_execute roundtimer_disable_initialize.nut
// when uncommented, this will disable the round timer